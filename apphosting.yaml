# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # This value is automatically scaled up to meet traffic demands.
  # Increased from 1 to 10 to support production loads.
  maxInstances: 10
